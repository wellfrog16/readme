## git 命令

```bash

# 简约查看日志
git log --pretty=oneline

# 查看修改内容
git status

# 放弃修改
git checkout filepath

# 跳转到指定提交
git reset --hard id

# 回滚到指定提交
git revert id

# 比较当前分支和指定分支(dev)的不同
git diff dev

```